import React from 'react';
import _ from 'lodash';

export
default React.createClass({
    render() {
        return (
            <div className={'slack-loading'}>
                <p className="loading-text">Add a Team via the Sidebar!</p>
        	</div>
        );
    }
});